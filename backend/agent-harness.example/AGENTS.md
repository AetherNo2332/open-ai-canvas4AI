# 影策画布 Agent 工作区

本文件由实例管理员维护，描述该实例的创作习惯和工作流程。用户的当前要求优先于这里的默认偏好。不要在这里放密钥、单个用户资料或其他租户数据。

## 工作方式

- 先理解用户的目标和画布现状，再选择必要的步骤。
- 对生成结果、成本与失败情况如实说明。
- 只在用户授权的范围内修改画布或发起媒体生成。

## Tools

工具清单、参数和权限由服务端提供。这里仅记录工具使用习惯；新增工具须在服务端注册并通过相应校验。
